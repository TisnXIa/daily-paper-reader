---
title: "fastQpick: scalable bootstrap and subsampling of FASTQ reads"
title_zh: "fastQpick: 可扩展的FASTQ读段自助法和子采样"
authors: "Rich, J., Pachter, L."
date: 2026-06-24
pdf: "https://www.biorxiv.org/content/10.64898/2026.06.23.734068v1.full.pdf"
tags: ["query:microbiome"]
score: 7.0
evidence: 可扩展的FASTQ读取自助抽样工具，适用于微生物组宏基因组数据
tldr: FASTQ reads的bootstrap采样可量化测序不确定性，但大规模数据需高效工具。fastQpick通过两遍流式处理实现有放回采样，默认先计数再写入，支持低内存和单遍模式。在5亿reads文库上30分钟内生成全尺寸bootstrap，峰值内存9.4 GB（低内存1.4 GB）。在酵母RNA-seq实验中，bootstrap副本恢复的丰度估计标准误与理论值匹配，验证了其有效性。
source: biorxiv
selection_source: fresh_fetch
motivation: 对FASTQ reads进行有放回采样，以在原始reads层面量化测序不确定性。
method: 采用两遍流式处理：先计数并建立哈希计数器，再写入样本，支持低内存和单遍O(1)内存模式。
result: 在5亿reads文库上30分钟内生成全尺寸bootstrap，峰值内存9.4 GB，低内存模式1.4 GB。
conclusion: bootstrap副本准确恢复转录本丰度估计的采样不确定性，与多项分布标准误匹配。
---

## 摘要
摘要：fastQpick 是一个命令行工具和Python库，用于对FASTQ读段进行有放回采样。有放回采样可以将单个FASTQ文件转换为任意数量的自助法重复，从而在原始读段水平上实现不确定性量化和统计分析。这一过程可以回答诸如：如果文库被重新测序，丰度估计值会变化多少？或者低丰度的检出是否对所测序的特定读段具有鲁棒性？fastQpick通过在默认情况下进行两遍流式处理来高效处理大型文库：首先统计读段数量并创建基于哈希的计数器，然后写入样本。它能在30分钟内生成一个5亿读段文库的全尺寸自助法重复，峰值内存为9.4 GB，并提供低内存模式将峰值降至1.4 GB。单遍模式在单次读取文件时抽取样本，使用常数级工作内存，产生的输出大小在期望上是精确的，但不是固定的。在一个真实的酵母RNA-seq实验中，由fastQpick生成的自助法重复重现了转录本丰度估计的采样不确定性，与多项标准误差的匹配误差在几个百分点之内。

可用性和实现：fastQpick 是开源软件，在MIT许可证下免费提供，GitHub地址为 https://github.com/pachterlab/fastQpick，也可通过PyPI安装（pip install fastQpick）。

## Abstract
Summary: fastQpick is a command-line tool and Python library for sampling FASTQ reads with replacement. Sampling with replacement turns a single FASTQ file into an arbitrary number of bootstrap replicates, which enables uncertainty quantification and statistical analysis at the level of raw reads. This process answers questions such as how much an abundance estimate would change if the library were resequenced, or whether a low-abundance call is robust to the particular reads that were sequenced. fastQpick works efficiently on large libraries by streaming files in two passes by default: first to count reads and create a hash-based counter, and then to write the sample. It generates a full-size bootstrap replicate of a 500-million-read library in under 30 minutes with 9.4 GB of peak memory, with a low-memory mode that reduces the peak to 1.4 GB. A single-pass mode draws samples in a single read through the file, using O(1) working memory and producing an output size that is exact in expectation but not fixed. In a real yeast RNA-seq experiment, bootstrap replicates generated by fastQpick recover the sampling uncertainty of transcript abundance estimates, matching the analytic multinomial standard errors to within a few percent.

Availability and implementationfastQpick is open source and freely available under the MIT license on GitHub at https://github.com/pachterlab/fastQpick and on PyPI (pip install fastQpick).